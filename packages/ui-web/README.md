# UI Web Package

Canonical role:

- web-only UI components and wrappers

Current files:

- `src/site-shell.js` for navigation, reveal, and simple form-shell behavior
- `src/index.js` as the package entrypoint

Current note:

- the marketing site still keeps a static copy of this behavior in
  `D:\Odirico\apps\web\marketing-site\script.js` so plain static deployment stays simple
