# API Package

Shared API-facing helpers used across Odirico app shells.

Current contents:

- Next.js request identity helpers
- JSON response helpers
- CORS/header helpers for route handlers

Planned additions:

- API client contracts
- fetch helpers
- service endpoint contracts shared across app shells
