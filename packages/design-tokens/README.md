# Design Tokens Package

Canonical role:

- shared brand tokens, spacing, color, typography, and motion primitives

Current files:

- `src/odirico.css` for browser and desktop CSS consumers
- `src/odirico.ts` for TypeScript app shells
- `src/index.ts` as the package entrypoint

Current consumers:

- `D:\Odirico\apps\mobile\odirico-mobile`
- `D:\Odirico\apps\desktop\odirico-desktop`
- mirrored manually by `D:\Odirico\apps\web\marketing-site\styles.css` until the public site adopts build tooling
